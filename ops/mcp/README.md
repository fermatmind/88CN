# MCP Examples

This directory contains example-only MCP configuration. It does not define a real server and must not contain credentials, private endpoints, public server addresses, or unrestricted filesystem roots.

Project MCP servers are disabled by default. A future 88CN MCP server may read only through the Public API allowlist and must not query Supabase directly.
