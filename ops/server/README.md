# Server Ops Examples

This directory stores redacted examples for deployment manifests. It must not contain real server addresses, environment values, private keys, certificate files, or credential-bearing URLs.

Use `production.local.example.json` as a shape reference only. Real local deployment notes stay outside Git.
