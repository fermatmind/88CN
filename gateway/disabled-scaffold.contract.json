{
  "schema_version": "1.0",
  "task_id": "PR92",
  "title": "Laravel Gateway Disabled Scaffold v0",
  "scaffold_status": {
    "documentation_only": true,
    "disabled_scaffold_only": true,
    "non_executable": true,
    "not_a_laravel_application": true,
    "no_composer": true,
    "no_artisan": true,
    "no_routes": true,
    "no_server_config": true
  },
  "required_flags": {
    "LARAVEL_GATEWAY_ENABLED": false,
    "LARAVEL_RUNTIME_ENABLED": false,
    "COMPOSER_DEPENDENCY_ENABLED": false,
    "PHP_RUNTIME_ENABLED": false,
    "SUPABASE_WEBHOOK_SYNC_ENABLED": false,
    "REDIS_GATEWAY_CACHE_ENABLED": false,
    "GATEWAY_CUSTOMER_ACCESS_ENABLED": false,
    "GATEWAY_API_KEY_RUNTIME_ENABLED": false,
    "GATEWAY_METERING_RUNTIME_ENABLED": false
  },
  "checkpoint_triggers": [
    "composer_dependency",
    "laravel_runtime",
    "php_runtime",
    "server_config",
    "live_deploy",
    "supabase_schema_change",
    "supabase_webhook_secret",
    "redis_production_usage",
    "customer_access",
    "api_key_runtime",
    "metering_runtime",
    "external_service_connection",
    "data_repo_mutation"
  ],
  "denied_file_classes": [
    "composer.json",
    "composer.lock",
    "artisan",
    "bootstrap/**",
    "routes/**",
    "**/*.php",
    "supabase/migrations/**",
    "deploy/**",
    ".env*"
  ],
  "denied_data_fields": [
    "founder_email",
    "contact_email",
    "admin_notes",
    "review_notes",
    "payment_state",
    "stripe_customer_id",
    "api_key_plaintext",
    "api_key_hash",
    "api_key_hash_salt",
    "bearer_credential",
    "session_credential",
    "service_role_credential",
    "raw_database_row",
    "raw_payload",
    "private_telemetry",
    "source_confidence_internal",
    "signal_score_internal",
    "claim_credential"
  ],
  "non_goals": [
    "No Laravel runtime.",
    "No Composer setup.",
    "No PHP runtime.",
    "No Supabase migration.",
    "No webhook runtime.",
    "No webhook secret.",
    "No Redis production config.",
    "No server config.",
    "No deploy.",
    "No dependency.",
    "No customer access.",
    "No API key runtime.",
    "No metering runtime.",
    "No data repo mutation.",
    "No PR93 work."
  ],
  "future_dependency_boundaries": {
    "PR91": "Laravel gateway boundary spec remains authoritative.",
    "PR81_PR82": "Future gateway output may only use reviewed public Alpha Feed snapshots.",
    "PR83": "Future sync must use approved one-way event-outbox contracts after checkpoint approval.",
    "PR87_PR88": "Future API key runtime requires a separate human checkpoint.",
    "PR89": "Future metering runtime requires a separate human checkpoint and remains separate from payment."
  }
}
